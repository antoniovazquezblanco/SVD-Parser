name: Release
on:
  push:
    tags:
    - v*

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
    - name: Create a release
      uses: ncipollo/release-action@v1.13.0
      with:
        allowUpdates: true
